// import { Test, TestingModule } from '@nestjs/testing';

describe('BassController', () => {
  // let guitarController: GuitarController;

  beforeEach(async () => {
    // todo implement later

    console.log('not implemented');

    //   const app: TestingModule = await Test.createTestingModule({
    //     controllers: [BassController],
    //     providers: [BassService],
    //   }).compile();

    //   bassController = app.get<BassController>(BassController);
    // });

    // describe('root', () => {
    //   it('should return "hej ty!"', () => {
    //     expect(bassController.getHello()).toBe('hej ty!');
    //   });
  });
});
